- [Aozora SQLite backend](aozora-backend.md) — api-server uses better-sqlite3 directly (not Drizzle/PostgreSQL); DB at cwd()/aozora.db; login field is "identifier" not "email"
- [react-native-maps web fix](rn-maps-web.md) — must use platform-specific files (DormMap.native.tsx / DormMap.tsx) to prevent web bundle failure; never import react-native-maps directly in a shared file
- [Expo babel-preset version](expo-babel.md) — babel-preset-expo must be ~54.0.10 for Expo 54; 56.x causes bundler failures
- [better-sqlite3 rebuild on Replit](better-sqlite3-rebuild.md) — native binding must be rebuilt after pnpm install; expo bin also needs direct path invocation
- [Admin routing architecture](admin-routing.md) — admin has own app/admin/_layout.tsx (Tabs, dark indigo theme); login redirects admin to /admin; root _layout lists "admin" as a single Stack.Screen group (not admin/index, admin/users etc.)
- [PageHeader component](page-header.md) — shared header at components/PageHeader.tsx; handles insets internally; fontSize 18/fontWeight "700"; props: title, subtitle?, showBack?, onBack?, right?; used on all admin + profile sub-screens
- [Aozora web serving](aozora-web-serving.md) — web app served as static export (expo export --platform web → artifacts/aozora/dist/); start.sh skips re-export if dist/index.html exists; dev-proxy.js on port 5000 serves dist/ directly + proxies /api to 8080
- [Aozora artifact workflow setup](aozora-artifact-workflows.md) — 4 artifact workflows: API Server (port 8080), Expo mobile (port 20823), mockup-sandbox (port 8081), Aozora Admin Website (port 5000 proxy); never add a custom API Server workflow — it conflicts with the artifact one on 8080
